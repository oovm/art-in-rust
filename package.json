{
    "private": true,
    "scripts": {
        "up": "cd projects/mondrian-rs && cargo publish --allow-dirty",
        "test": "cargo test --release",
        "fmt": "cargo fmt --all",
        "push": "wee test && git push",
        "reset": "git reset Head~ --soft",
        "dev": "cd projects/mondrian-web && trunk serve"
    }
}
